query FetchInAppChatMessages(
  $conversationId: UUID!
  $limit: Int
  $page: Int
) {
  fetchInAppChatMessages(
    input: {
      conversationId: $conversationId
      limit: $limit
      page: $page
    }
  ) {
    ...MessageFragment
  }
}
